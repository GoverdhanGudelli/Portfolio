// Navigate between pages
function navigate(page) {
  window.location.href = page;
}

// Display project info (demo)
function showProjectDetails(name) {
  alert(`You clicked on: ${name}`);
}
